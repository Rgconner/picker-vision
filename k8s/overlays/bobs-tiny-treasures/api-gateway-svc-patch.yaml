---
# Pin the api-gateway LoadBalancer to .213 from the shared sandbox pool.
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  annotations:
    metallb.universe.tf/loadBalancerIPs: 192.168.11.213
    metallb.universe.tf/address-pool: sandbox
spec:
  type: LoadBalancer
